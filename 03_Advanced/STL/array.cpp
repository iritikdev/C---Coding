#include<bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[]) {
    /*
    classic Array : int nums[] = {7,8,9,6,2};

    STL array : static in nature but with more functionality and generic
    array<int, 5> nums {1,2,3,4,5};

    */
    // array<datatype, size> variable = {elements};
    array<int, 5> nums = {4, 5, 6, 9, 3};

    /*
    Member function of array template
    at() or [] operator
    front() & back()
    begin() & end()
    fill()
    swap() : swap two array(same type, same size)

    empty() : used to check whether the array is empty or not.

    size()
    */
    return 0;
}
